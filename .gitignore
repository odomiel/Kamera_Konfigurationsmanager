# Python
__pycache__/
*.pyc
*.pyo

# Build / Packaging (AppImage + PyInstaller, analog Discovery-Tool)
build/
dist/
*.exe
*.AppImage
.tk9build/
.tk9build_log.txt
squashfs-root/
appimagetool*.AppImage
base.AppImage

# Lokale Claude-Code-Einstellungen (maschinenspezifisch, inkl. temporärer
# Schreibdateien wie settings.local.json.tmp.*)
.claude/settings.local.json*

# Lokale Testdaten (ADM-Konfigurationsdateien, Firmware-Images)
*.cfg
*.bin

# Projektspezifische Claude-Notiz (nicht versioniert, wie im Discovery-Tool)
CLAUDE.md

# Lokale Fehler-Screenshots (nicht versionieren)
fehler/
curl_firmware_test.txt

# Lokale AXIS-Device-Manager-Exporte (enthalten Klartext-Zugangsdaten!)
axisdevicemanager_export/
